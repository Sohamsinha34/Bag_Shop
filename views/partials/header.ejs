<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-[helvetica_now_display']">
    <nav class="w-full flex justify-between px-3 py-5 ">
        <h3 class="text-2xl tracking-wide text-gray-500">STACH</h3>
        <a href="/profile" class="text-sm  text-red-600 px-2 py-2  block text-left w-fit">
              Create Account</a>
        <% var loggedin = typeof loggedin !== "undefined"? loggedin : true %>
        <% if(loggedin){ %>
        <div class="flex gap-5">
            <a href="/shop">Shop</a>
            <a href="/cart">Cart</a>
            
            <a href="/display-profile">My Account</a>
            <a class="text-red-600" href="/logout">Logout</a>
        </div>
        <% } %>
    </nav>
    
